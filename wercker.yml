box: busybox
build:
  box: webratio/ant
  steps:
    - script:
        name: build
        code: |
          echo $WERCKER_CACHE_DIR
    - script:
        name: build
        code: |
          ./build.sh
deploy:
  box: nated/wercker-base
  steps:
    - add-to-known_hosts:
        hostname: scp.hidrive.strato.com
        fingerprint: 2a:7d:3a:04:bd:df:02:7f:05:4b:ea:8b:32:00:47:91 
    - add-ssh-key:
        keyname: hidrive-ssh

