box: haroon/docker-oracle-jdk7
build:
  steps:
    - script:
        name: install ant
        code: |
          apt-get install -y -f ant
    - script:
        name: build
        code: |
          sh ./build.sh

