box: scottweston/ant-oracle
build:
  steps:
    - script:
        name: install ant
        code: |
          apt-get install -y -f ant
    - script:
        name: build
        code: |
          sh ./build.sh

