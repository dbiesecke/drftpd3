box: webratio/ant
build:
  steps:
    - script:
        name: build
        code: |
          ./build.sh
deploy:
    steps:
        - github-create-release:
            token: $GITHUB_TOKEN
            tag: v1.0.0
        - github-upload-asset:
            token: $GITHUB_TOKEN
            file: build.tgz
